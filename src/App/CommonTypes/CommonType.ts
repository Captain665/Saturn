import React from "react";

export interface userInfo {
    createdAt: string;
    emailId: string;
    fullName: string;
    gender: string;
    id: number;
    jwt: string;
    lastLogin: string;
    mobileNumber: string;
    password: string;
    role: string;
    updatedAt: string;
}